#!/bin/bash

echo Rebuilding database
rm -rf var/sqlite
mkdir -p var/sqlite
migrate

echo Creating user: $(whoami)
uwiki-user -p password $(whoami)

echo Creating initial pages
for page in etc/pages/*; do
    echo $(basename $page)
    uwiki-page edit $(basename $page) < $page
done
